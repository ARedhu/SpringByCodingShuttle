spring.application.name=SpringSecurity
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.datasource.url=jdbc:mysql://localhost:3306/SpringSecurity?allowPublicKeyRetrieval=true&useSSL=false
# useSSL=true in production
spring.datasource.username=root
spring.datasource.password=Ashu@Golu1

# port where our current application is running.
server.port = 9000


# Security config
# Here we can set the username and password for login in spring application. But this method of login form is not used in production.
#spring.security.user.name=Ashish
#spring.security.user.password=pass

jwt.secretKey=3451rhjdhfiwrj3h4r34rh4uhr9973nyugry4

deploy.env=development

# We will take the authorization key and secret from the google and put these here.
# oauth2 credentials.
#spring.security.oauth2.client.registration.google.client-id=
#spring.security.oauth2.client.registration.google.client-secret=

# Here we are repeating log of things in the names of constants. A better idea is to create a "yml" file.
